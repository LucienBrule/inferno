[project]
name = "inferno-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lucien Brule", email = "lucien@lucienbrule.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
inferno-tools = "inferno_tools:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
